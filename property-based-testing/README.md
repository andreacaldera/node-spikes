# Property based testing in Node.js

## Usage

### Run the sample application:
`npm start`

### Run tests:
`npm test`

Tests includes an example of propery based testing and will output a test coverage report to the file `coverage.html`